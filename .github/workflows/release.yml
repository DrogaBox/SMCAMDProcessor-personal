name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build all targets
    runs-on: macos-15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer

      - name: Download Lilu & VirtualSMC dependencies
        run: |
          LILU_URL=$(curl -sL https://api.github.com/repos/acidanthera/Lilu/releases/latest | grep "browser_download_url" | grep -i "release.zip" | cut -d '"' -f 4)
          curl -L -o Lilu.zip "$LILU_URL"
          unzip -q Lilu.zip -d Lilu_extracted
          mv Lilu_extracted/Lilu.kext .
          rm -rf Lilu.zip Lilu_extracted

          SMC_URL=$(curl -sL https://api.github.com/repos/acidanthera/VirtualSMC/releases/latest | grep "browser_download_url" | grep -i "release.zip" | cut -d '"' -f 4)
          curl -L -o VirtualSMC.zip "$SMC_URL"
          unzip -q VirtualSMC.zip -d SMC_extracted
          mv SMC_extracted/Kexts/VirtualSMC.kext .
          rm -rf VirtualSMC.zip SMC_extracted

      - name: Build AMDRyzenCPUPowerManagement.kext
        run: |
          xcodebuild \
            -workspace SMCAMDProcessor.xcodeproj/project.xcworkspace \
            -scheme AMDRyzenCPUPowerManagement \
            -configuration Release \
            SYMROOT="$PWD/build" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            clean build

      - name: Build SMCAMDProcessor.kext
        run: |
          xcodebuild \
            -workspace SMCAMDProcessor.xcodeproj/project.xcworkspace \
            -scheme SMCAMDProcessor \
            -configuration Release \
            SYMROOT="$PWD/build" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Build AMD Power Gadget.app
        run: |
          xcodebuild \
            -workspace SMCAMDProcessor.xcodeproj/project.xcworkspace \
            -scheme "AMD Power Gadget" \
            -configuration Release \
            SYMROOT="$PWD/build" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package binaries
        run: |
          TAG="${GITHUB_REF_NAME}"
          mkdir -p "Binaries_Release/$TAG"
          cp -R "build/Release/AMDRyzenCPUPowerManagement.kext" "Binaries_Release/$TAG/"
          cp -R "build/Release/SMCAMDProcessor.kext"             "Binaries_Release/$TAG/"
          cp -R "build/Release/AMD Power Gadget.app"             "Binaries_Release/$TAG/"
          zip -r "Binaries_Release_${TAG}.zip" "Binaries_Release/$TAG"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "${{ github.ref_name }}"
          generate_release_notes: true
          files: |
            Binaries_Release_${{ github.ref_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
